OPENAI_API_KEY=sk-...

# Secret dùng để ký/xác thực token phê duyệt qua email (mục /decision trong api.py).
# PHẢI đặt cùng 1 giá trị này trong Apps Script: Extensions → Apps Script →
# Project Settings → Script Properties → key "DECISION_SECRET".
# Sinh 1 chuỗi ngẫu nhiên đủ dài, KHÔNG dùng giá trị mẫu này.
DECISION_TOKEN_SECRET=
